supergraph:
  # The socket address a`nd port to listen on
  listen: 127.0.0.1:4000
  path: /graphql
include_subgraph_errors:
  all: true # Propagate errors from all subgraphs
  subgraphs:
    products: false # Do not propagate errors from the products subgraph
